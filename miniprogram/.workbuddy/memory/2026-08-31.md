## 2026-08-31 续画「发现」「签到」两屏
- 在同一 Ardot 文件（720508756716625）x=980 / x=1470 补齐发现页、签到页视觉稿，与前两屏同风格（暗色 #111114 / 卡 #1E1E23 / 橙 #FF5A36 / Noto Sans SC）。
- 发现页：最新/热门下划线切换、3 张动态卡（单图点赞态 / 双图 / 纯文字约局）、胶囊 tabbar（发现高亮）。
- 签到页：三统计卡、2026年8月日历（已签到橙字、今天橙圆、翻月箭头）、打卡记录（场地/店铺类型 chip）、本月排行榜（自己行橙底高亮、layoutGrow 右对齐次数）、tabbar 签到高亮。
- 踩坑记录：① 中途 ADAPTER_DISCONNECTED，报错批次实际已落盘，重试前先 batch_read 检查残留；② 容器级 opacity 会稀释子内容，tint 一律用 fills[i].opacity。
- 至此四屏（首页/发现/签到/详情）视觉稿齐全；下一步候选：我的页、或按稿改 wxss 落代码。
## 2026-08-31 设计理念评审修正落画布
- 对照用户四原则评审：视觉/轻量达标，字号与操作路径有差距。
- 画布修正：① 全稿扫读字号≥12px（卡片标签 10→12、tab 标签 10→11）、评分数字 13→15px；② 首页三卡加「快捷签到」按钮（active 橙 tint / 已签到置灰描边）；③ 新增「交互规范标注」卡（x=1960）：切实体保留搜索词、快捷签到 2 步内、字号下限约定。
- 代码侧待办：switchEntity 保留 query、卡片快捷签到直开打卡弹窗。
## 2026-08-31 「我的」页视觉稿完成
- 依据 profile.js/wxml 实际数据结构绘制（x=2360，节点 7:28）：资料卡（头像/昵称/滑龄/城市/编辑箭头）+ 新增数据小结三卡（签到42橙/报错2/建议3，即菜单计数的可视化前置）+ 菜单五项（签到/报错/你提我改带计数，场地管理/设置）+ 最近签到三条 + 胶囊tabbar（我的高亮）。
- 五屏视觉稿全部齐：首页 2:2 / 详情 2:166 / 发现 5:2 / 签到 5:115 / 我的 7:28 / 交互规范卡 7:13（x=0/490/980/1470/2360）。
## 2026-08-31 详情页签到打卡弹窗视觉稿
- 两块新画布：① 7:147「详情·签到弹窗态」(x=2850)——复制详情页压暗 + 底部面板（把手/标题+关闭/地点chip/留言框+0-200计数/照片格带删除与添加格/渐变CTA确认签到/取消）；② 7:311「签到成功·庆祝态」(x=3340)——庆祝卡（橙环对勾/签到成功/连续6天青绿/徽章激励文案）+ 六颗彩色纸屑。
- 对应代码：venue-detail.wxml 签到弹窗 + celebrate；升级点：底部 sheet 化（原为居中卡片）、地点确认 chip、徽章激励文案。
## 2026-08-31 修正：庆祝卡去多余文案
- 用户反馈"再打 1 天解锁徽章"属多余内容 → 删除 7:319，庆祝卡仅留「签到成功 + 已连续打卡 6 天」真实反馈。新增设计原则第 5 条（内容克制）入 MEMORY.md。
## 2026-08-31 「查看全部打卡」列表页视觉稿
- 7:327 (x=3830)，对应代码 place-checkins：导航行（返回+标题+场地名）、计数"共 36 条"、打卡卡（作者/内容/图/评论入口三种状态：收起5条/展开2条+回复输入/0条）、上拉加载提示。
- 遵守内容克制原则，无额外模块。
## 2026-08-31 「我的」去多余 + 四个子页面视觉稿
- 删除「我的」页数据小结三卡（7:59，代码中无此结构），页面回归代码忠实：资料卡+菜单+最近签到。
- 新增三屏：我的报错 7:405 (x=4320)、你提我改 7:449 (x=4810，含提一条 FAB)、场地管理 7:490 (x=5300，四分类tab+城市筛选+行操作编辑/删除+新增按钮)。
- 我的签到=switchTab 到签到 tab 页（已有 5:115），未重复设计。
- 教训复现：tint 色块再次出现 node-level opacity 问题，批量插入后需统一用真实 ID 修 fills[i].opacity。
## 2026-08-31 「新增场地」表单视觉稿
- 7:560 (x=5790)：复制场地管理页 + 遮罩 + 底部表单 Sheet。字段对照 admin.wxml：名称输入、城市/类型双选择器、标签多选 chips、坐标"打开地图选点"描边按钮、地址、照片格(≤6)、热门开关(橙)、渐变保存按钮。
- 踩坑：gradient fills JSON 过长会 parse 失败，改 fill 简写后重插并 M() 调顺序。

## 08-31 审计收尾（交互链路核对）
- Ardot 画布连接在本会话不可用（工具搜索不到 ardot MCP），新画 4 屏（店铺详情 7:725 / 场地报错弹窗 7:913 / 评分弹窗 / 编辑资料弹窗）的截图验证待连接恢复后补做。
- 交互链路核对（基于代码）：主链路全部打通——签到链路（详情→弹窗→庆祝态→已签到·补充+卡片角标）、评分（两详情页均有入口）、报错（场地详情→我的报错/admin）、打卡流（查看全部→回详情）、我的→4子页+编辑资料弹窗（各页均有 onShow 刷新）。
- 发现问题：① shop-detail 无「报错」入口，与 venue-detail 不对称（报错类型含「信息变更」适合店铺）；② place-checkins 点卡片 navigateTo 叠栈（详情→全部→详情），可接受；③ 设计稿「首页快捷签到」为新增项（用户已确认保留），代码未实现，闭环可行（venue-card 已签到角标代码已有）。

## 08-31 代码 vs 设计 全量核对（本次会话）
- 通读全部 10 页面 + 3 组件 + custom-tab-bar + store.js，对照 Ardot 视觉稿记忆做逐页元素/理念/交互核对。
- 最大发现【主题冲突】：视觉稿为暗色街头风（#111114 底/#1E1E23 卡），代码实为浅色主体（app.wxss page bg #F5F5F5、venue-card #FFFFFF），仅 nav/tabbar 暗色（#1A1A1E）。属系统性偏差，需用户定夺方向。
- 理念合规偏差：① 绿 #00D4AA 越界用于「已签到」（venue-detail .action-done line525、venue-card .badge-checked #00B386 line46），超出"仅限在场/在线"；② 存在 20rpx(=10px)/22rpx(=11px) 小字号（角标/标签/tab），设计修正要求≥24rpx(12px)；③ card-in 0.4s 超 250ms 微动效上限；④ profile "解锁专属身份" 属营销式文案（profile.wxml:18），违"内容克制"。
- 新发现 bug：profile 最近签到未携带 kind，goVenue 固定跳 venue-detail（profile.js:43-47/141），店铺签到会误跳"场地不存在"。checkins/discover/place-checkins 均已正确按 kind 分流，唯 profile 漏。
- 设计缺失：10 页视觉稿全齐，仅 shop-detail(7:725) 及店铺侧弹窗"已画未核对"（Ardot MCP 本轮仍不可用）。无"未设计"页面。
- 待决策项：主题方向 / 绿色语义 / "解锁专属身份"文案去留 / shop-detail 补报错入口 / profile 误跳修复。

## 08-31 设计稿换通道交付
- 用户无法访问原 Ardot 画布（ardot.tencent.com/file/720508756716625，本会话 ardot MCP 工具始终未注入，面板/浏览器均受阻）。
- 改用本地 HTML 高保真重建：`.workbuddy/outputs/skatespot-design-mockups.html`，17 屏全部重绘为手机框预览（核心6页+子页5页+弹窗5个+规范卡），内容为审计定稿版（榜单「累计」、地址错误、无营销文案、庆祝态真实数据）。
- 备注：店铺报错入口、首页快捷签到为待开发项，页面底部已标注。

## SkateSpot 全量 UI/UE 审计
- 通读全部 10 页面 + 3 组件 + custom-tab-bar，产出审计报告。
- P0：列表卡片无照片（venue-card/shop-card 未用 photos 字段）；设计令牌漂移（#F7F7F8/#00B578/#FF9F2E/#6B6B70 等游离值）；双详情页形态不一致（底部操作栏/庆祝图标/轮播高度）；详情页无首屏 loading（venue 为 null 时白屏）；discover onShow 无条件 reload 丢滚动位置。
- P1：折叠手柄对比度不足、badge-hot 坐标 hack、ASCII 箭头当图标、城市弹层无遮罩、日历不可翻月、签到无 vibrateShort、地图 pin 场地/店铺不可区分、列表无排序。
- 下一步：用户可能要求出高保真改版稿（照片化场地卡 + 统一详情页）。

## 08-31 按定稿设计稿落地代码（2 个功能缺口）
- **店铺详情补报错入口**：shop-detail 底部操作栏加「报错」按钮 + 报错弹窗（复用页面 mask/sheet 样式与打卡弹窗结构，浅色主题内保持一致）；类型 chips 同 venue-detail 的 REPORT_TYPES 五类，店铺默认「信息变更」；提交走 cloud.addVenueReport，新增 `kind:'shop'` 字段落 venue_reports。
- **首页卡片快捷签到**：venue-card 在「此刻 N 人」行右侧加橙色「签到」胶囊（catchtap 防冒泡、已签到隐藏）；点击 navigateTo venue-detail?auto=checkin，venue-detail onLoad 检测参数且当日未签到时直接 openCheckin()。
- **消费端按 kind 路由**：reports.js（我的报错）与 admin.js（场地管理-报错 tab）映射加 kind 字段，goVenue/goVenueDetail 按 kind==='shop' 跳 shop-detail，否则 venue-detail；reports.wxml 文案改「场地/店铺详情页」。
- 验证：5 个改动 JS 全部 node --check 通过；代码现状为浅色主题（设计稿为暗色街头风，全量换肤待用户确认后另做）。

## SkateSpot 高保真改版设计稿（画布）
- 文件：ardot.tencent.com/file/720719275880664
- 三屏：① 首页改版（照片化场地卡 + 深色 header + 胶囊 tabbar）；② 场地详情改版（沉浸式封面 + 统一底部操作栏 + 深色“在场”卡）；③ 发现页改版（深色 header + 社区照片流）。
- 设计关键词：深色/水泥灰 #17181C + 品牌橙 #FF5A36 + 在场绿 #00D4AA；统一圆角 16/24、统一阴影；中文 Noto Sans SC / 数字 Inter。
- 视觉验证：三张截图均通过，删掉首页第三张完全出屏卡片，保留前两张 + 自然裁切暗示可滚动。

## 08-31 全站暗色化落地（按本地 HTML 设计稿）
- 依据 `.workbuddy/outputs/skatespot-design-mockups.html` 完成全站换肤，纯视觉层，wxml/js 未动。
- token 映射：page bg #F5F5F5→#111114、card #FFF→#1E1E23、inset→#17171B、line→#26262C、sheet/modal→#1B1B20、主橙 #FF5A36（渐变 CTA 135deg→#FF8A6E）、绿 #00D4AA 仅在场/营业中/已签到/已处理语义、文字五级 #FFF/#D9D9DE/#A6A6AD/#8C8C94/#6E6E76、tint：橙 rgba(255,90,54,.14-.16)/绿 rgba(0,212,170,.12-.14)/红 rgba(255,59,48,.14)/蓝 rgba(42,140,255,.14)。
- 改动清单：app.json（backgroundColor #111114 + backgroundTextStyle light）、app.wxss 全重写（page token 块，旧变量名 --ink/--ash 等翻转兼容）、16 个 wxss（10 页面 + venue-card/shop-card/comment-box）、custom-tab-bar 已暗色未动、6 个页面 json 的 backgroundTextStyle dark→light（sed）。
- 验证全通过：全局 grep 无残留浅色背景/深色文字；全部 json node JSON.parse 校验 "json ok"。
- 重要：上轮功能项（首页快捷签到、店铺报错入口、reports/admin 按 kind 路由）此前已被用户回滚，本次未重新加入——如需恢复另行确认。
- 待用户在微信开发者工具中逐页预览。
