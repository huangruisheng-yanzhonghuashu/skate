# 2026-09-10

## 发现页媒体改「通栏直角」（用户提方案 → 选 A）
- 背景：discover-mockup.html feed 卡媒体原为 rounded-2xl(16px)/九宫格 rounded-xl(12px)，与 rounded-3xl 卡片双层圆角嵌套，精致感与街头气质冲突。给出 A 通栏直角 / B 内嵌直角 / C 缩小圆角 6px 三方案，用户选 A。
- 实施（discover-mockup.html）：
  - 单图/视频（4:3）×6（feed×4、搜索卡×2）：rounded-2xl → `-mx-5` 通栏出血直角；
  - 帖子详情页媒体：`-mx-6`（详情页内容 p-6，通栏贴屏幕边）；
  - 双图行：gap-3 → gap-1，去圆角，`-mx-5` 通栏；
  - 九宫格：gap-2 → gap-1，去 rounded-xl，`-mx-5` 通栏。
  - grep 复核：rounded-2xl/rounded-xl 媒体零残留。
- 规范同步：DISCOVER_DESIGN_SPEC.md 5.3 媒体区新增「通栏直角原则」——媒体直角、出血贴满卡宽、圆角只属于卡片容器；间距统一 4rpx；搜索卡与详情页同规则。
- 首页（design-mockup.html）媒体未改：首页打卡流卡片保持圆角媒体，两页形成「首页收、发现页放」的节奏差异；如需首页统一再说。

## 详情页「在场卡」改为「今日打卡卡」（三方案 → 用户选 A）
- 用户需求：「此刻 6 位滑手在此 / 近 30 分钟暂无滑手在场」→「今日多少位滑手打卡」。给 A 热度卡（左橙线+头像行）/ B 数据面板卡（大数字+全部›）/ C 并入信息卡 三方案，选 A。
- 实施（venue-detail-mockup.html）：
  - 有数据态：「此刻 6 位滑手在此」→「今日 6 位滑手打卡」纯排版（今日/位滑手打卡 13px #8C8C94，数字 16px 600 白）；
  - 空态：「近 30 分钟暂无滑手在场」→「今日还没滑手打卡」；
  - 左线 #00D4AA → #FF5A36（有数据）/ #3A3A42（空态）；删除 .pulse 脉冲点（CSS+HTML 两处）；
  - 头像行保留（语义=今日打卡滑手，数据源从实时定位改为打卡记录）；.l-ava.c4 头像底色 #00D4AA → #FF8A6E（避绿）。
  - grep 复核：pulse 零残留；剩余 #00D4AA 全为已签到合法语义（done 键/庆祝层）。
- 规范同步（VENUE_DETAIL_DESIGN_SPEC.md）：标题改「今日打卡卡」+语义变更注记（实时在前台下线）；2.1 树、5.1/5.2、空态表、姊妹页营业状态卡措辞、动效表删脉冲行、版本表加 v1.1。
- 产品级影响：实时在场功能从产品前台彻底消失（首页/列表/详情页均改打卡数据），落码时后端在场逻辑可清理。

## 全站头像改圆角方形（三方案 → 用户选 A）
- 用户问首页最新打卡/发现列表的圆形头像能否换形状。给 A 圆角方形 / B 贴纸风（描边+微旋转）/ C 六边形（螺母隐喻）三方案，选 A。
- 实施：头像类 `rounded-full` → 圆角方形，radius≈边长 25-30%：
  - discover-mockup.html：w-11(44px) 头像 ×9 → `rounded-[10px]`（feed 头部×5、详情页×1、评论区 w-10 ×3、搜索卡×2）；评论区 w-10 ×3 同 radius；
  - design-mockup.html：最新打卡小头像 w-5(20px) ×2（杰/K）→ `rounded-[6px]`；
  - 误伤回滚：design-mockup 地图次级 pin 圆头（MapSheetPin2Head, w-5 h-5）被批量替换波及，已恢复 rounded-full。
  - 保留圆形：播放按钮、地图 pin 头、FAB、按钮/输入框/开关等控件，以及场地标签胶囊（控件/胶囊语言不动）。
- 规范同步：DISCOVER_DESIGN_SPEC.md（feed 头像 72rpx 圆角方形 20rpx、评论区 56rpx/16rpx + 全站方角语言原则）；DESIGN_SPEC.md 8.1 模块 4 补首页小头像规则（圆角≈边长 30%）。

## Krug 可用性体检落地（用户挂载 dianshi-chengjin-laws skill → 体检 → 确认 P1①/P2/P3）
- 体检结论：三页主点击流全部零思考 ✅；揪出 3 个问题。
- P1①（首页）：城市打卡数据「今日 862 人打过卡」去掉胶囊边框/底色 → 纯文字数据不可点（消除「像按钮但点了没反应」歧义）。
- P2（首页）：砍掉「最新打卡」区副文案「实时社区」（第三定律：无信息氛围词）。
- P3（首页场地卡×2 + 详情页头图×2）：「热门」贴纸全部改为数据贴纸「本周 N 人来滑」（1240/902/38），禁止无口径的「热门」二字。
- 规范同步：DESIGN_SPEC.md（8.1 模块3 纯文字规则、模块4 无副文案、倾斜数据贴纸规则）；VENUE_DETAIL_DESIGN_SPEC.md（热门贴纸条件从「在场≥5」改「本周来滑≥10」+文案规格）。
- 教训沉淀：①数据文字若用胶囊/按钮形态包装，必须可点或去掉形态；②贴纸/标签文案必须带口径（时间窗+数字），无口径的氛围词（热门/实时/精选）是问号制造机。
