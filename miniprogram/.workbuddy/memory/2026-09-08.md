# 2026-09-08

## 详情页高保真稿修复：报错入口与距离 pill 重叠（用户反馈「详情页中报错，别距离遮住了」）
- 问题：`design/venue-detail-mockup.html` 报错入口 absolute 定位在信息卡右上角，压住评分行右侧「距你 1.2km」pill。
- 修复（画板 A/B 同步）：
  - HTML：报错入口从 info-card 顶部移入 `.addr-row` 行尾；地址文字包 `<span class="addr-text">`（flex:1 长地址自动换行避让）。
  - CSS：`.report-entry` 由 absolute 改 flex 项（flex-shrink:0，margin-top 3px 对齐地址首行）；新增 `.addr-text { flex:1; min-width:0 }`。
  - 规范同步 3 处：`VENUE_DETAIL_DESIGN_SPEC.md` 2.1 信息架构树、4.5 节、7.1 备注——位置统一改为「地址行行尾」。
- 教训：absolute 角标入口在信息卡内易与行尾元素（pill）冲突，行内 flex 末位更稳。
- 待办（用户确认规范后落码）：详情页按规范 v1.0 落码（含庆祝层删激励文案）、新建 video-preview 页面并注册 app.json、发现页视频点击改跳转、修 data-media dataset 传参 bug。

## 复发与再修复：报错入口被画布同步覆盖
- 用户反馈「地址行行尾（现状）现在 ui 上没有」→ 排查确认：venue-detail-mockup.html 带设计画布双向同步（元素带 data-page-node-id），上一轮的 addr-row 结构修改被画布同步覆盖——画板 A 的报错入口整个丢失、画板 B 退回右上角旧位置（删除/插入类编辑均可能被回滚）。
- 已重新修复两画板（addr-row 行尾插入 report-entry + addr-text 包裹），并 grep 复核：report-entry 仅 3 处（CSS + A/B 各一处，均在 addr-row 内）。
- 教训：编辑该 mockup 后必须 grep 复核落盘结果；提醒用户刷新已打开的设计画布，避免旧内容再次覆盖。
- 术语澄清：「现状」指新稿版式；线上小程序 UI 报错在底部操作栏（四键之一），A 是规范提案的迁移位置。
- 报错入口选址讨论：给出 A 地址行行尾（新稿）/ B 页尾小字链接（美团点评模式，推荐候选）/ C 长按地址呼出（叠加增强）/ D 右上角·悬浮键（禁区）四方案，待用户定夺。
