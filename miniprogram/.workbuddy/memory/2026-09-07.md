## 发现页内容预览 UI 设计 v1.3
- 用户反馈：视频预览返回应与首页场地详情一致，右下角操作组应为「点赞 / 评论 / 转发」，而非「点赞 / 关闭 / 转发」。
- 设计决策：
  - 视频预览不放置任何关闭按钮，退出走系统返回 / 左侧滑回，与首页场地详情返回方式一致。
  - 视频右下角竖排三个圆形操作按钮：点赞（心形）、评论（气泡 + 数量）、转发（分享）。
  - 评论按钮显示评论数量，数量为 0 时隐藏数字只显示气泡图标。
  - 图片预览保持不变：点击画面关闭、多图时顶部计数器 + 底部圆点。
  - 顶部继续禁止任何自定义按钮，避免与小程序系统胶囊/返回区冲突。
- 改动：
  - 更新 `design/MEDIA_VIEWER_DESIGN_SPEC.md` 为 v1.3。
  - 更新 `design/media-viewer-mockup.html`，视频预览画板改为点赞/评论/转发操作组。
  - 同步更新 `design/DISCOVER_DESIGN_SPEC.md` 第 5 条交互规则。
- 本次只输出 UI 设计，未修改代码。

## 视频预览发表评论 UI 设计 v1.4
- 用户要求：给出视频预览中发表评论的 UI 设计。
- 设计决策：
  - 点击视频预览右下角「评论」按钮，从底部拉起半屏评论面板（bottom-sheet）。
  - 面板默认占屏幕 60%，可拖拽上拉至 80%，背景遮罩 rgba(0,0,0,0.55)，点击遮罩关闭。
  - 面板背景 `#1E1E23`，顶部圆角 32rpx，包含顶部把手、标题「N 条评论」、关闭 ×。
  - 评论列表项：头像 64rpx、昵称、时间、内容、右侧点赞数（已点赞橙色）。
  - 底部输入区：圆角输入框「说点什么…」+ 发送按钮；无内容时置灰，有内容时橙色可点击。
  - 空态：评论数为 0 时列表居中显示「暂无评论，来说点什么吧」。
- 改动：
  - 更新 `design/MEDIA_VIEWER_DESIGN_SPEC.md` 为 v1.4，新增第 4 节「评论面板」。
  - 更新 `design/media-viewer-mockup.html`，新增「视频预览 + 评论面板」画板。
- 本次只输出 UI 设计，未修改代码。

## 视频预览改为独立页面 v1.5（重要架构决策）
- 用户反馈：视频预览应如首页列表进入场地详情页面一样，导航栏有返回按钮，支持右滑返回。
- 设计决策（关键）：
  - **视频预览 = 独立页面** `pages/video-preview/video-preview`，navigateTo 跳转，不再是发现页内遮罩。
  - **图片预览 = 页面内遮罩** `media-viewer` 组件，点击图片关闭，保持不变。
  - 视频页自定义透明导航栏：左侧白色返回按钮（88×88rpx 触控区），右侧系统胶囊保留区不放任何元素。
  - 返回三方式：导航栏返回按钮 / iOS 右滑手势 / Android 系统返回键（与场地详情页一致）。
  - 页面参数 URL query 传 postId，页面内拉取数据，避免 URL 传超长 cloud:// 路径。
  - 返回时点赞数/评论数回写发现页 feed 项。
  - 评论面板保持在视频页内作为 bottom-sheet，面板遮罩覆盖导航栏，先关面板再返回。
- 为什么独立页面：视频有播放状态+评论等重交互，独立页面有完整生命周期与系统返回手势，且避免遮罩内自绘返回按钮与右上角胶囊冲突。
- 改动：
  - 更新 `design/MEDIA_VIEWER_DESIGN_SPEC.md` 为 v1.5（结构调整：第 3 节改为独立页面，新增载体对比表与胶囊避让原则）。
  - 更新 `design/media-viewer-mockup.html`，视频画板加导航栏（返回按钮+胶囊区虚线示意）。
  - 同步更新 `design/DISCOVER_DESIGN_SPEC.md` 第 5 条交互规则。
- 本次只输出 UI 设计，未修改代码。落代码时需新建 video-preview 页面并注册到 app.json。

## 场地详情页 UI 审计 v1.0
- 用户要求：在首页 UI 基础上对详情页做设计迭代，先全面分析 + 给修改建议，必须符合产品/设计理念。
- 审计产出 `design/VENUE_DETAIL_AUDIT.md`，关键发现：
  - **P0 理念硬伤**：签到庆祝层含「再打 N 天解锁徽章」激励文案（celebrateSub / STREAK_MILESTONES=[3,7,30,100]），与规范 2.5「无营销式激励文案」逐字冲突；且彩纸+2.6s 强制停留违反轻量化（微动效 ≤250ms）。
  - P1 信息层级：头图 50vh（min 600rpx）过高，地址/距离/在场跌出首屏；评分叠在 swiper 上可点击，与滑图手势冲突。
  - P2 视觉态度：标签全灰（首页 venue-card 规则是首标签橙色高亮，详情页未延续）；在场卡绿底绿边铺色过度，绿色应只做语义点缀。
  - P3 结构：报错低频却占操作栏 1/4；键位随签到态跳动；评分弹窗居中而打卡/报错是底部 sheet，形态不统一；打卡卡与发现页 feed 卡不同构；运营方行与父卡同色无分界。
- 修改建议：头图降 38vh、评分下移与距离同行、首标签橙色、在场卡改左绿线、报错挪出操作栏变固定三键、评分改底部 sheet、打卡卡对齐发现页、庆祝层瘦身（只留签到成功+连续天数）。
- shop-detail 与 venue-detail 同构，改造时共用规范。
- 本轮只出分析与建议，未出高保真稿、未改代码。

## 场地详情页设计规范 + 高保真稿 v1.0
- 用户确认审计方向后，产出：
  - `design/VENUE_DETAIL_DESIGN_SPEC.md` v1.0：信息架构（头图38vh/信息卡决策行/在场卡/打卡动态/固定三键操作栏）、弹层统一底部 sheet、庆祝层瘦身（无彩纸无激励文案、≤250ms、1.2s 自动消失）、状态规则汇总、shop-detail 复用差异、禁止项。
  - `design/venue-detail-mockup.html`：4 个画板——A 未签到主状态（首屏决策信息全可见+热门贴纸+首橙标签+左绿线在场卡）、B 已签到（主键=发打卡/副键=已签到绿描边，键位不跳；无人在场灰线弱化态）、C 庆祝层瘦身版、D 评分底部 sheet。
- 关键设计决策：
  - 报错从操作栏迁入信息卡右上角（弱化为最浅灰小字）。
  - 操作栏固定三键槽位：左导航128rpx/中主键flex/右副键128rpx，键位不随签到态跳动。
  - 首屏验收硬指标：iPhone 标准屏不滚动看到名称/评分/距离/地址/≥1标签/在场状态。
  - 热门贴纸条件：在场 ≥5 人，名称行右侧 rotate(-6deg)。
  - 评分从图上移到信息卡第一行（与距离 pill 组成决策行），点击开底部 sheet 评分。
- 未改代码，待用户确认后落码。

## 打卡卡「同构双版本」设计决策
- 用户提问：详情页打卡动态与发现页列表是否要设计成一样。
- 结论：**骨架统一，密度与交互按场景裁剪**——同一打卡实体不能两种长相（认知/维护成本翻倍），但发现页=刷内容互动消费、详情页=场地决策看氛围，任务不同。
- 详情页 compact 版裁剪两项：
  - 去场地胶囊（已是当前场地，自指冗余）。
  - 去互动区（点赞/评论）：详情页操作焦点唯一（签到/导航/发打卡），且只展示 3 条样本太小互动数无意义。
- 互动动线：详情页紧凑卡 → 查看全部 place-checkins（完整版可互动）→ post-detail。
- 实现建议：打卡卡做成同一组件两模式（full | compact），compact 不渲染胶囊与互动区。
- 已固化进 `VENUE_DETAIL_DESIGN_SPEC.md` 第 6.0 节。

## 签到页（Tab）UI 设计 v1.0
- 用户要求：在首页 UI 基础上对签到页做设计，需全面分析现有页、给出 UI/UX 修改建议、符合产品理念与设计理念。
- 关键分析结论：
  - 模块顺序（统计带 → 日历 → 榜单 → 记录）符合「记录与社区比较」任务，但页面缺少态度区，进入感冷。
  - 统计带/日历/榜单卡片无描边，与首页 `map-entry` / `feed-card` 体系不一致。
  - 已签日期仅白字 + 6rpx 橙点，户外扫读性差；我的行使用橙底橙边，与已签到语义色（绿）冲突。
  - 记录区无分组头，空态文案「去场地或店铺签到吧」与产品规则冲突（签到页本身不是入口）。
  - 下拉刷新未设 `backgroundColor`，存在露白风险。
- 设计决策：
  - 新增页面态度区：大标题 + 连续天数 slogan（真实数据）+ 品牌橙短条，延续首页街头情绪。
  - 所有卡片统一加 1rpx `#26262C` 描边；统计数字放大到 44rpx。
  - 已签日期改为橙实心圆；今日未签 = 橙描边空心圆；今日已签 = 橙实心圆 + 右下角小白点。
  - 我的行改为青绿左缘（已签到语义）。
  - 最近记录增加 `sec-head` 分组头，与首页「最新打卡」对齐。
  - 空态统一使用「去看看附近场地」引导回首页，禁止出现全局「去签到」CTA。
  - `checkins.json` 增加 `"backgroundColor": "#111114"` 防止下拉露白。
- 改动：
  - 新增 `design/CHECKINS_DESIGN_SPEC.md` v1.0。
  - 新增 `design/checkins-mockup.html`，包含 A. 有数据状态、B. 新用户空态两个画板。
- 本次只输出 UI 设计，未修改代码。

## 我的页 UI 设计 v1.0
- 用户要求：在首页 UI 基础上对我的页做设计，需全面分析、给出 UI/UX 建议、符合产品理念。
- 关键分析结论：
  - 原生导航栏已显示「我的」，页面内又放 44rpx 大标题「我的」，信息冗余。
  - 个人卡片无描边，与首页卡片体系不一致。
  - 城市、滑龄各占一行，加上标签和完善提示，首屏被快速撑满。
  - 「点击完善资料，解锁专属身份」含激励式文案，违反「内容克制」。
  - 菜单 hover 用橙底色，与首页 `opacity: 0.7` 按压态不统一。
  - 编辑资料用居中卡片，而首页地图/详情页评分/报错均用底部 sheet，形态割裂。
- 设计决策：
  - 去掉页面内重复大标题，保留原生导航栏「我的」。
  - 所有卡片加 `1rpx solid #26262C` 描边。
  - 城市与滑龄合并为一行（`城市 · 滑龄`），减少纵向空间。
  - 完善提示文案改为「点击完善资料」，去掉营销化表述。
  - 数据快捷带数字放大到 44rpx；仅签到数使用品牌橙，其余白色。
  - 菜单增加分组头「我的参与」「管理」，按压态统一 `opacity: 0.7`。
  - 编辑资料从居中卡片改为底部 sheet（85vh、32rpx 顶部圆角、把手、标题 + 右上角保存），与首页/详情页弹层统一。
- 改动：
  - 新增 `design/PROFILE_DESIGN_SPEC.md` v1.0。
  - 新增 `design/profile-mockup.html`，包含 A. 资料完善、B. 新用户未完善、C. 编辑资料 Sheet 三个画板。
- 本次只输出 UI 设计，未修改代码。

## 签到页 + 我的页 v2 重构（重要方向修正）
- 用户反馈：对 v1 设计「十分不满意」。根因：v1 只是给旧页面加描边/合并行/补小态度区，仍是「中性报表/系统设置」气质，违背规范 2.6「不因大众化而变成中性模板」。
- v2 核心方法（对后续页面通用）：
  - **态度头与首页 brand-wordmark 同构**：子页用 72rpx 斜体大字标（首页 84rpx 降一档）+ 橙色 slogan + 品牌橙短条。签到页=「轨迹 / 把每天留在街头。」，我的页=「档案 / 这是你的轨迹。」。
  - **立 Hero 不立卡**：签到页删三列统计带，改 120rpx 斜体橙 Hero 数字（连续天数）+ 一行次数据；我的页数据带去卡化（48rpx 斜体、签到橙、其余白）。
  - **贴纸语言**：倾斜描边贴纸（rotate -6deg，每页 ≤1 组）承载真实数据——签到页 STREAK 贴纸（streak≥3 才显示）、我的页擅长标签改描边贴纸。
  - **身份牌（Rider Pass）**：我的页个人卡加顶部 8rpx 橙渐变卡缘 + 144rpx 橙环头像 + 48rpx 斜体昵称；空身份牌也保留橙条。
  - **自定义导航**：两页都改 navigationStyle: custom，左侧留空，右侧胶囊保留区虚线避让。
  - **绿色纪律修正**：签到页记录行「场地」徽章从 #00D4AA 改灰（绿仅在场/已签到语义）；「我的行」青绿左缘保留（已签到语义合规）。
- 产出：
  - `design/CHECKINS_DESIGN_SPEC.md` v2.0（含 v1→v2 对照表）。
  - `design/checkins-mockup.html` v2（A 有数据 / B 新用户空势态）。
  - `design/PROFILE_DESIGN_SPEC.md` v2.0。
  - `design/profile-mockup.html` v2（A 资料完善 / B 新用户 / C 编辑资料 Sheet）。
- 本次只输出 UI 设计，未修改代码。
